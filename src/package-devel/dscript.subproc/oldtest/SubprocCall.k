//Written by Joseph
//import("konoha.new"); Don't use this
import("dscript.subproc");

void test() {
	// assert(Subproc.call("ls -l") == 0);
	// assert(Subproc.call("echo hello") == 0);
	// assert(Subproc.call("exit 1") == 1);

	// Subproc s = new Subproc("echo hi", false);
	// assert(s.call("echo hoge") == 0);
	// s.fg();
	// //after execution
	// assert(s.call("echo fuga") == 0);
}

test();
