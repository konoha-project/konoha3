// written by kimio kuramitsu
import("konoha.new")
import("Syntax.JavaStyleClass")
import("JavaScript.Array");

void test() {
	Array a = new Array(10);  // Empty
	assert(a.getSize() == 0);
}

test();

