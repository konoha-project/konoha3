// written by kimio

import("JavaStyle.Object");

void test() {
	boolean b;
	int n;
	String s;
	Func f;
	assert(!(b instanceof void));
	assert(!(n instanceof void));
	assert(!(s instanceof void));
	assert(!(f instanceof void));
}

test();

